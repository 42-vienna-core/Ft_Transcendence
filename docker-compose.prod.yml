services:
  frontend:
    build:
      context: ./frontend
      target: prod  
    volumes: !override [] 

  backend:
    build:
      context: ./backend
      target: prod  
    volumes:
      - avatars:/uploads/avatars